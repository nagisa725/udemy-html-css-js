let hello = "heloo world";
console.log(hello.length);
// length 文字列の長さを取得できる
// toUpperCase() 文字列が大文字表記になる
// substring(0,?) 与えられた文字列分だけ表記する
// slice 同上
// split() 文字を分割する,空文字だと一文字ずつ表記される
// 文末に()が付くものをメソッドという特定の処理を行うもの
// ()で終わらないものをプロパティと言い、定義された変数の特定の値を保持しているもの